apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: tenants
  namespace: stage-flux-system
spec:
  interval: 5m
  sourceRef:
    kind: GitRepository
    name: stage-flux-system
  path: ./tenants/pacific-aks
  prune: true
  validation: client